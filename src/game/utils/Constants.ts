export const LANES = [-3, -1, 1, 3] as const;

export const GAME_CONFIG = {
  baseSpeed: 0.25,
  maxSpeed: 1.2,
  speedIncrement: 0.0005,
  baseSpawnRate: 1.8,
  minSpawnRate: 0.5,
  spawnAcceleration: 0.001,
} as const;

