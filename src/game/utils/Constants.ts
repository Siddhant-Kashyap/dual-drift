export const LANES = [-3, -1, 1, 3] as const;

export const GAME_CONFIG = {
  baseSpeed: 0.45,
  maxSpeed: 1.6,
  speedIncrement: 0.0007,
  baseSpawnRate: 1.1,
  minSpawnRate: 0.22,
  spawnAcceleration: 0.0022,
} as const;

